<Page
    x:Class="OneSale.Views.PlansPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneSale.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBox x:Name="tbxUrl" Width="800"/>
            <Button x:Name="btnGo" Content="go" Click="btnGo_Click"/>
            <TextBox x:Name="tbxAmount" Width="100" Text="80"/>
            <Button x:Name="btnClear" Content="Clear" Click="btnClear_Click"/>
        </StackPanel>
        <TextBox x:Name="tbxResult" Height="600" TextWrapping="Wrap" Text="{x:Bind result, Mode=TwoWay}"/>
    </StackPanel>
</Page>
